---
title: Resources Test Page
---

import { graphql } from 'gatsby';
import { colors } from 'gatsby-theme-apollo-core';
import breakpoints from '../utils/breakpoints.js';
import ResourceList from '../components/ResourceList';


export const query = graphql`
  query ResourcesPageQuery {
    allResource {
      nodes {
        id
        title
        subtitle
        author
        category
        priority
        type
        url
        image
        pubYear
        tags
        description
      }
    }
  }
`

export default ({ children, data }) => (
  <div>
    {children}
    <ResourceList resources = {data.allResource.nodes} />
  </div>
);

<div style={{ 
  textAlign: 'center',
}}>

<h2 style={{
  color: colors.text1,
}}>
Data science for product managers </h2>

<h4 style={{
  color: colors.primary,
}}>
A top-down overview of data science aimed at product managers.
</h4>

</div>